package com.dreamgames.backendengineeringcasestudy.converter;
/*
import com.dreamgames.backendengineeringcasestudy.domain.TestGroup;
import org.springframework.core.convert.converter.Converter;
import org.springframework.data.convert.WritingConverter;
import org.springframework.stereotype.Component;

// enum to TINYINT

@WritingConverter
public class TestGroupWritingConverter implements Converter<TestGroup, Integer> {
    @Override
    public Integer convert(TestGroup testGroup) {
        return testGroup != null ? testGroup.getCode() : null;
    }
}
*/
