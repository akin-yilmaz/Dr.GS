package com.dreamgames.backendengineeringcasestudy.converter;
/*
import com.dreamgames.backendengineeringcasestudy.domain.TestGroup;
import org.springframework.core.convert.converter.Converter;
import org.springframework.data.convert.ReadingConverter;
import org.springframework.stereotype.Component;

// TINYINT to enum

@ReadingConverter
public class TestGroupReadingConverter implements Converter<Integer, TestGroup> {
    @Override
    public TestGroup convert(Integer source) {
        return (source != null) ? TestGroup.fromCode(source) : null;
    }
}
*/