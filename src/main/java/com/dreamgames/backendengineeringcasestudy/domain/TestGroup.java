package com.dreamgames.backendengineeringcasestudy.domain;

public enum TestGroup {
    /*
    A(0),
    B(1);

    private final int code;

    TestGroup(int code) {
        this.code = code;
    }

    public int getCode() {
        return code;
    }

    public static TestGroup fromCode(int code){
        for (TestGroup testGroup : TestGroup.values()) {
            if (testGroup.getCode() == code) {
                return testGroup;
            }
        }
        throw new IllegalArgumentException("Invalid status code: " + code);
    }
    */
    A,
    B;
}
